# RAG smoke-test sample

For repeatable manual E2E (upload + ingest + Q&A), use the canonical PDF:

- **Preferred location in the repo:** `dev-data/samples/user-agreement-7-1.pdf`
- **Source on your machine (if not committed):** copy from the project root  
  `user-agreement-7-1.pdf` into this folder.

Example questions after upload: clauses, parties, termination, or any heading visible in the document.

The file is optional in git (binary); this folder documents the expected name and path for the team.
